nodeLinker: node-modules
npmPublishAccess: public

# Defaults (60s timeout x 3 retries) let a single hung/blocked request eat
# several minutes before failing - fail faster so a curation 403 or a
# genuine network hang doesn't burn the whole CI job budget.
httpTimeout: 30000
httpRetry: 2
